[build-system]
requires = ["setuptools", "wheel", "torch"]
build-backend = "setuptools.build_meta"

[project]
name = "diff-surfel-rasterization"
dependencies = []
requires-python = ">=3.8"
authors = []
maintainers = []
description = ""
readme = "README.md"